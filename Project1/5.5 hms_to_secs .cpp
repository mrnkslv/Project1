
/*#include <iostream>
using namespace std;
long hms_to_secs(int,int,int);
int main() {
	setlocale(LC_ALL, "Rus");
	int h, m, s;
	char ch = 'a';
	cout << "Для начала работы программы введите любой символ, кроме n :";
	cin >> ch;
	while (ch != 'n') {
		cout << "Введите часы, минуты и секунды: ";
		cin >> h >> m >> s;
		cout << "Результат в секундах:  " << hms_to_secs(h, m, s)<<endl;
		cout << "Для продолжения работы программы введите любой символ, кроме n : ";
		cin >> ch;
	}
	return 0;
}
long hms_to_secs(int h, int m, int s) {
		return h*3600+m*60+s;
}
*/