/*#include <iostream>
using namespace std;
int main() {
	setlocale(LC_ALL, "Rus");
	struct point {
		int x;
		int y;
	};
	point p1, p2, sum;
	cout << "Введите координаты точки p1 : ";
	cin >> p1.x >> p1.y;
	cout << "Введите координаты точки p2 : ";
	cin >> p2.x >> p2.y;
	sum.x = p1.x + p2.x;
	sum.y = p1.y + p2.y;

	cout << "Координаты точки p1+p2 равны: " << sum.x<< ". " << sum.y <<endl;
	return 0;
}*/